# Database Configuration
# PostgreSQL connection string format:
# DATABASE_URL="postgresql://[user]:[password]@[host]:[port]/[database]?schema=public"
# 
# For local development:
# 1. Install PostgreSQL (https://www.postgresql.org/download/)
# 2. Create a database: createdb viz_db
# 3. Update the connection string below with your credentials
DATABASE_URL="postgresql://username:password@localhost:5432/viz_db?schema=public"

# NextAuth Configuration
# Base URL for the application (used by NextAuth for callbacks)
NEXTAUTH_URL="http://localhost:3000"

# NextAuth secret key (used to encrypt JWT tokens)
# Generate a secure secret with: openssl rand -base64 32
# IMPORTANT: Use a different secret in production!
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# Optional: Environment
# NODE_ENV="production"

# Optional: Email/SMS for verification (future feature)
# SMTP_HOST=""
# SMTP_PORT=""
# SMTP_USER=""
# SMTP_PASS=""
# SMS_API_KEY=""

